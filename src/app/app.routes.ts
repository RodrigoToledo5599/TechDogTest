import { Routes } from '@angular/router';
import { SearchAddress } from './features/search-address/search-address';


export const routes: Routes = [
    {
        path: '',
        component: SearchAddress
    }
];
